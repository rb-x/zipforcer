# Python Zip bruteforcer

A simple python brute force script for protected zip files

## Usage :

1. in the same directory put a zipfile and a dictionary list
2. run the script `zipforcer.py -f <zipfile> -d <dictionary>`
